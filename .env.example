# Public host name for HTTPS. Without your own domain, use the VM's public IP
# with nip.io, e.g. 149.165.1.2.nip.io — Caddy obtains a real certificate for it.
DOMAIN=

# Login required on every page. Choose something long.
APP_PASSWORD=
APP_USER=admin
